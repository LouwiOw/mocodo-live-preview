# mocodo-live README

This extension provides live rendering of [Mocodo](https://mocodo.net/) diagrams in Visual Studio Code.

## Requirements

You need to have python installed and the `mocodo` package available in your environment. You can install it using pip:

```bash
pip install mocodo
```

